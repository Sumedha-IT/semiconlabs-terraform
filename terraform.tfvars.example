# Production manual apply — use terraform.tfvars.production.example as the template.
# Staging labs: staging-labs-tf repository (not this repo).
#
# lab_environment = "production"   # required here
# Backend Start Lab writes tfvars from .env — see .env.production.example

# Copy to terraform.tfvars (gitignored) and adjust. Current golden lab AMI (ap-south-1, DCV watcher baked in):
ami_id = "ami-035d56902b1949f48"

# Optional overrides — defaults also live in variables.tf
# instance_type = "m6a.xlarge"
# subnet_id     = "subnet-0c4d6990bb08a09b2"
